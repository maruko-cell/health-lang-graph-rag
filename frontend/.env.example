# 前端环境变量模板（复制为 .env 后按需修改，勿将 .env 提交到版本库）
# Vite 仅会加载以 VITE_ 开头的变量

# 后端 API 基地址（必填）
VITE_BACKEND_ORIGIN=http://localhost:4000

# 当前用户 ID 占位（可选，未设置时使用 localStorage / default_user）
# VITE_USER_ID=default_user
